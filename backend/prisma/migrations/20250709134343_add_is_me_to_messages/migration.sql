BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[messages] ADD [isMe] BIT NOT NULL CONSTRAINT [messages_isMe_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
